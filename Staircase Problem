#include <stdio.h>

#include <string.h>

int main(){

    int n,i; 

    printf("Enter the no of rows:");

    scanf("%d",&n);

    char str[]="";

    for(i=0;i<n;i++)

        strcat(str,"#");

    for(i=0;i<n;i++)

        printf("%*.*s\n",n,i+1,str);

    return 0;

}
